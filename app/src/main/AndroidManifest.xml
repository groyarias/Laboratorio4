<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lab04">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab04">
        <activity android:name=".activities.ListComprasActivity"></activity>
        <activity android:name=".activities.ComprarVueloActivity" />
        <activity android:name=".activities.ContraseniaActivity" />
        <activity
            android:name=".activities.RegistroActivity"
            android:label="Registro usuario" />
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SecondActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lab04.NoActionBar" />
        <activity android:name=".activities.ListVuelosActivity" />
    </application>

</manifest>